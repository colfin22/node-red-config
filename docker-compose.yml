services:
  node-red:
    image: nodered/node-red:latest
    container_name: node-red
    ports:
      - "1880:1880"
    environment:
      - TZ=Europe/Dublin
    env_file:
      - .env
    volumes:
      - /opt/node-red/data:/data
    restart: unless-stopped
